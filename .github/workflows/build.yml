name: Build StatusShift

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:
  release:
    types: [published]

jobs:
  build:
    runs-on: windows-2022
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v5
        with:
          ref: ${{ github.event.release.target_commitish || github.ref }}

      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 10.0.x

      - name: Read version from csproj
        id: ver
        shell: pwsh
        run: |
          [xml]$cs = Get-Content StatusShift/StatusShift.csproj
          $v = $cs.Project.PropertyGroup.Version | Select-Object -First 1
          if (-not $v) { throw "Version missing from StatusShift.csproj" }
          $unix = [DateTimeOffset]::UtcNow.ToUnixTimeSeconds()
          echo "version=$v" >> $env:GITHUB_OUTPUT
          echo "unix=$unix" >> $env:GITHUB_OUTPUT
          Write-Host "Version $v  LastUpdate $unix"

      - name: Download Dalamud
        shell: pwsh
        run: |
          $dest = "$env:AppData\XIVLauncher\addon\Hooks\dev"
          New-Item -ItemType Directory -Force -Path $dest | Out-Null
          Invoke-WebRequest -Uri https://goatcorp.github.io/dalamud-distrib/latest.zip -OutFile dalamud.zip
          Expand-Archive -Force dalamud.zip $dest

      - name: Build
        run: dotnet build StatusShift.slnx --configuration Release -p:RestoreLockedMode=false

      - name: Stage package
        shell: pwsh
        run: |
          $dll = Get-ChildItem -Recurse -Filter StatusShift.dll |
            Where-Object { $_.FullName -match "\\bin\\" } |
            Select-Object -First 1
          if (-not $dll) {
            Get-ChildItem -Recurse StatusShift | Select-Object FullName
            throw "StatusShift.dll missing"
          }
          $out = "artifacts/StatusShift"
          New-Item -ItemType Directory -Force -Path $out | Out-Null
          Copy-Item "$($dll.DirectoryName)\*" $out -Recurse -Force
          New-Item -ItemType Directory -Force -Path "$out\images" | Out-Null
          $icon = $null
          foreach ($candidate in @(
            "images\icon.png",
            "$($dll.DirectoryName)\images\icon.png",
            "$($dll.DirectoryName)\icon.png"
          )) {
            if (Test-Path $candidate) { $icon = $candidate; break }
          }
          if (-not $icon) { throw "icon.png not found to package" }
          Copy-Item $icon "$out\images\icon.png" -Force
          Copy-Item $icon "$out\icon.png" -Force
          if (-not (Test-Path "$out\StatusShift.dll")) { throw "Copy failed" }
          Compress-Archive -Path "$out\*" -DestinationPath artifacts/StatusShift.zip -Force
          Write-Host "Packaged $($dll.FullName) as artifacts/StatusShift.zip"

      - uses: actions/upload-artifact@v4
        with:
          name: StatusShift
          path: |
            artifacts/StatusShift/**
            artifacts/StatusShift.zip
          if-no-files-found: error

      - name: Sync pluginmaster and repo.json
        if: github.event_name == 'release'
        shell: pwsh
        run: |
          $v = "${{ steps.ver.outputs.version }}"
          $unix = [int64]"${{ steps.ver.outputs.unix }}"
          $link = "https://github.com/XozaShadow/StatusShift/releases/latest/download/StatusShift.zip"
          foreach ($path in @("pluginmaster.json", "repo.json")) {
            $raw = Get-Content $path -Raw | ConvertFrom-Json
            $entry = if ($raw -is [System.Array]) { $raw[0] } else { $raw }
            $entry.AssemblyVersion = $v
            $entry.DownloadLinkInstall = $link
            $entry.DownloadLinkUpdate = $link
            $entry.DownloadLinkTesting = $link
            if ($entry.PSObject.Properties.Name -contains "TestingAssemblyVersion") {
              $entry.TestingAssemblyVersion = $v
            }
            if ($entry.PSObject.Properties.Name -contains "LastUpdate") {
              $entry.LastUpdate = $unix
            }
            $json = ConvertTo-Json -InputObject @($entry) -Depth 8
            if (-not $json.TrimStart().StartsWith('[')) {
              $json = "[`n" + $json + "`n]"
            }
            [IO.File]::WriteAllText((Resolve-Path $path), $json.TrimEnd() + "`n")
          }
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add pluginmaster.json repo.json
          git diff --cached --quiet
          if ($LASTEXITCODE -ne 0) {
            git commit -m "release: $v manifests [skip ci]"
            git push origin HEAD:main
          }

      - name: Attach StatusShift.zip to release
        if: github.event_name == 'release'
        uses: softprops/action-gh-release@v2
        with:
          files: artifacts/StatusShift.zip
